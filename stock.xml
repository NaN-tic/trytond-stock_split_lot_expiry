<?xml version="1.0"?>
<!-- The COPYRIGHT file at the top level of this repository contains the full
     copyright notices and license terms. -->
<tryton>
    <data>
        <!-- stock.move -->
        <record model="ir.ui.view" id="move_view_form">
            <field name="model">stock.move</field>
            <field name="inherit" ref="stock.move_view_form"/>
            <field name="name">move_form</field>
        </record>

        <record model="ir.ui.view" id="move_view_list">
            <field name="model">stock.move</field>
            <field name="inherit" ref="stock.move_view_tree"/>
            <field name="name">move_list</field>
        </record>

        <record model="ir.model.button" id="move_split_by_expiry_date_button">
            <field name="name">split_by_expiry_date</field>
            <field name="model" search="[('model', '=', 'stock.move')]"/>
        </record>
        <record model="ir.model.button-res.group"
            id="move_split_by_expiry_date_button_group_stock">
            <field name="button" ref="move_split_by_expiry_date_button"/>
            <field name="group" ref="stock.group_stock"/>
        </record>

        <!-- stock.shipment.out -->
        <record model="ir.ui.view" id="shipment_out_view_form">
            <field name="model">stock.shipment.out</field>
            <field name="inherit" ref="stock.shipment_out_view_form"/>
            <field name="name">shipment_out_form</field>
        </record>

        <record model="ir.model.button"
            id="shipment_out_split_by_expiry_date_button">
            <field name="name">split_moves_by_expiry_date</field>
            <field name="model"
                search="[('model', '=', 'stock.shipment.out')]"/>
        </record>
        <record model="ir.model.button-res.group"
            id="shipment_out_split_by_expiry_date_button_group_stock">
            <field name="button"
                ref="shipment_out_split_by_expiry_date_button"/>
            <field name="group" ref="stock.group_stock"/>
        </record>
    </data>
</tryton>
